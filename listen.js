//module to listen for updates from connected devices found via search loop

//----------------------------
//Init function starts the listen loop
//----------------------------
module.exports.init = function(callback) {
	//Begin listening for device updates

	//Fire callback with success
	callback(null,null);
}

